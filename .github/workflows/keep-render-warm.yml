name: Keep Render Warm

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render worker
        run: |
          curl -sS --max-time 20 https://notesquared-c.onrender.com/health > /dev/null
